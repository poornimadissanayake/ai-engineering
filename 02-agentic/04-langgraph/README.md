# Module 4 — LangChain & LangGraph

Agents as state graphs: nodes, conditional routing, checkpointing and durable execution that survives a failure mid-run.

**Projects:** Sidekick browser agent

## Running this

```bash
cp ../../.env.example ../../.env   # if not done already
uv sync
```

Setup notes per project go here as they are built.
