# Module 6 — Model Context Protocol

MCP hosts, clients, servers and transports; writing my own server and wrapping existing tools as MCP resources.

**Projects:** My own MCP server · Trading Floor capstone

## Running this

```bash
cp ../../.env.example ../../.env   # if not done already
uv sync
```

Setup notes per project go here as they are built.
