# Module 2 — Multimodal chatbot

Frontier APIs, Gradio UI, and a customer-service / multimodal agent.

**Projects:** Gradio chatbot

## Running this

```bash
cp ../../.env.example ../../.env   # if not done already
uv sync
```

Setup notes per project go here as they are built.
