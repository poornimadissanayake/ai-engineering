# Module 6 — Data curation & frontier fine-tune

Curating a dataset, then fine-tuning a frontier model and writing down what changed.

**Projects:** Curated dataset · frontier fine-tune

## Running this

```bash
cp ../../.env.example ../../.env   # if not done already
uv sync
```

Setup notes per project go here as they are built.
