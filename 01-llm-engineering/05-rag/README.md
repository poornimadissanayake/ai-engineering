# Module 5 — RAG

Embeddings, vector stores, LangChain, and an end-to-end knowledge-worker app.

**Projects:** RAG knowledge worker

## Running this

```bash
cp ../../.env.example ../../.env   # if not done already
uv sync
```

Setup notes per project go here as they are built.
