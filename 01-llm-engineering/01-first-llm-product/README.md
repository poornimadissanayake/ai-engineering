# Module 1 — First LLM product

Environment, Chat Completions API, prompting, and comparing frontier models. Ends with a runnable website summarizer.

**Projects:** Website summarizer

## Running this

```bash
cp ../../.env.example ../../.env   # if not done already
uv sync
```

Setup notes per project go here as they are built.
