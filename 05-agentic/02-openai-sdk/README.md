# Module 2 — OpenAI Agents SDK

The same loop as module 1, done with a mature SDK: agents, runners, sessions, tracing, structured outputs, guardrails, handoffs.

**Projects:** Sales/outreach team · Deep Research agent

## Running this

```bash
cp ../../.env.example ../../.env   # if not done already
uv sync
```

Setup notes per project go here as they are built.
