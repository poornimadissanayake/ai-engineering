# Module 1 — Foundations

Agents built from scratch: raw tool calling, the agent loop, state and memory by hand. No agent framework imported anywhere in this folder — that is the point.

**Projects:** Career Digital Twin (deployed)

## Running this

```bash
cp ../../.env.example ../../.env   # if not done already
uv sync
```

Setup notes per project go here as they are built.
