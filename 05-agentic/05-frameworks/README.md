# Module 5 — Other frameworks

AutoGen, Google ADK, AWS Strands and Pydantic AI, compared on the same task so the choice can be deliberate.

**Projects:** Agent Creator · framework decision matrix

## Running this

```bash
cp ../../.env.example ../../.env   # if not done already
uv sync
```

Setup notes per project go here as they are built.
