# Module 4 — Model selection & code gen

Evaluating models for code generation and business tasks, then picking one with a written rationale.

**Projects:** Code-generation showdown

## Running this

```bash
cp ../../.env.example ../../.env   # if not done already
uv sync
```

Setup notes per project go here as they are built.
