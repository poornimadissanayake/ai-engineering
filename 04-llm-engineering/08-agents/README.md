# Module 8 — Autonomous multi-agent capstone

The Core Track closer: a multi-agent system that solves a commercial problem, plus the course certificate.

**Projects:** Autonomous agent capstone

## Running this

```bash
cp ../../.env.example ../../.env   # if not done already
uv sync
```

Setup notes per project go here as they are built.
