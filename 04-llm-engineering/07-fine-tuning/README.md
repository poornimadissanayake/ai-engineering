# Module 7 — QLoRA on open-source

QLoRA fine-tune of an open-source model, then a bake-off against the base model and a frontier model.

**Projects:** QLoRA notebook · eval bake-off

## Running this

```bash
cp ../../.env.example ../../.env   # if not done already
uv sync
```

Setup notes per project go here as they are built.
