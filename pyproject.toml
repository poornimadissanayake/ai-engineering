[project]
name = "ai-engineering"
version = "0.1.0"
description = "Working through Ed Donner's AI engineering curriculum — Core, Agents, Production."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "beautifulsoup4>=4.12.0",
    "gradio>=4.0.0",
    "ipykernel>=6.29.0",
    "jupyter>=1.0.0",
    "openai>=1.40.0",
    "python-dotenv>=1.0.0",
    "requests>=2.32.0",
]
